input AMPLIFY {
  globalAuthRule: AuthRule = { allow: public }
}

type TranslationMessage @model {
  id: ID!
  # TranslationQueue#timestamp
  sortKey: String!
  # Cognito username of the interpreter
  interpreter: String!
}

enum TranslationQueue {
  EngGer
  EngPt
}
